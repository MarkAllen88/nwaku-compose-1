name: MarkoP2Pworld

services:

  postgres:
    image: postgres:15.4-alpine3.18
    restart: on-failure
    shm_size: "1g"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./postgres_cfg/postgresql.conf:/etc/postgresql/postgresql.conf:Z
      - ./postgresql:/var/lib/postgresql/data:Z
    command:
      - postgres
      - -c
      - config_file=/etc/postgresql/postgresql.conf
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d postgres"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  postgres-exporter:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    restart: on-failure
    environment:
      DATA_SOURCE_NAME: >-
        postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/postgres?sslmode=disable
    ports:
      - "9187:9187"
    volumes:
      - ./postgres_exporter.yml:/etc/postgres_exporter.yml:ro
    command:
      - "--config.file=/etc/postgres_exporter.yml"
    depends_on:
      postgres:
        condition: service_healthy

  nwaku:
    image: wakuorg/nwaku:v0.36.0
#    image: wakuorg/nwaku:v0.33.0
    restart: unless-stopped
    env_file: .env
    ports:
      - "30304:30304/tcp"
      - "30304:30304/udp"
      - "60000:60000"   # WebSocket (ws)
      - "60002:60002"   # libp2p TCP
      - "8645:8645"     # REST
      - "9091:9091"     # Metrics
    volumes:
      - ./keystore:/keystore:Z
      - ./data:/data
    command:
#      - --cluster-id=1
      - --lightpush=true
      - --listen-address=0.0.0.0
      - --log-level=DEBUG
      - --metrics-server-address=0.0.0.0
      - --metrics-server-port=9091
      - --metrics-server=true
      - --nodekey=57adf02621712672818636f30e8c191dd7adc29222c7c4c78c22c71797bb2e43
#      - --preset=twn
#      - --pubsub-topic=/waku/2/rs/1/0
      - --relay-peer-exchange=true
      - --relay=true
      - --rest-address=0.0.0.0
      - --rest-admin=true
      - --rest-port=8645
      - --rest=true
#      - --rln-relay-chain-id=59141
#      - --rln-relay-cred-password=${RLN_RELAY_CRED_PASSWORD}
#      - --rln-relay-cred-path=/keystore/keystore.json
#      - --rln-relay-dynamic=false
#      - --rln-relay-eth-client-address=${RLN_RELAY_ETH_CLIENT_ADDRESS}
#      - --rln-relay-eth-contract-address=${RLN_CONTRACT_ADDRESS}
#      - --rln-relay-tree-path=/data/rln_tree
      - --rln-relay=false
      - --staticnode=/dns4/nwaku2/tcp/60003/p2p/16Uiu2HAm9F3d75PFrM4KF8HA6EuKbSQBfjTKhZi4dLMxtoLcNufu
      - --store-message-db-url=${STORE_MESSAGE_DB_URL}
      - --store=true
      - --tcp-port=60002
      - --websocket-port=60000
      - --websocket-support=true
#      - --discv5-bootstrap-node=enr:-QESuED0qW1BCmF-oH_ARGPr97Nv767bl_43uoy70vrbah3EaCAdK3Q0iRQ6wkSTTpdrg_dU_NC2ydO8leSlRpBX4pxiAYJpZIJ2NIJpcIRA4VDAim11bHRpYWRkcnO4XAArNiZub2RlLTAxLmRvLWFtczMud2FrdS5zYW5kYm94LnN0YXR1cy5pbQZ2XwAtNiZub2RlLTAxLmRvLWFtczMud2FrdS5zYW5kYm94LnN0YXR1cy5pbQYfQN4DgnJzkwABCAAAAAEAAgADAAQABQAGAAeJc2VjcDI1NmsxoQOTd-h5owwj-cx7xrmbvQKU8CV3Fomfdvcv1MBc-67T5oN0Y3CCdl-DdWRwgiMohXdha3UyDw
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9091/metrics || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    dns:
      - 8.8.8.8
      - 8.8.4.4

  nwaku2:
    image: wakuorg/nwaku:v0.36.0
#    image: wakuorg/nwaku:v0.33.0
    restart: unless-stopped
    env_file: .env
    ports:
      - "60003:60001"   # WebSocket (ws) for the second node (host 60003 → container 60001)
      - "60004:60003"
      - "30305:30305/tcp"
      - "30305:30305/udp"
      - "8646:8646"
      - "9092:9092"
    volumes:
      - ./keystore:/keystore:Z
      - ./data2:/data
    command:
#      - --cluster-id=1
      - --lightpush=true
      - --listen-address=0.0.0.0
      - --log-level=DEBUG
      - --metrics-server-address=0.0.0.0
      - --metrics-server-port=9092
      - --metrics-server=true
      - --nodekey=32ee547b3decdb2a326625d1af592c695181fc219cf8571bd3b1e029963ffb6b
#      - --preset=twn
#      - --pubsub-topic=/waku/2/rs/1/0
      - --relay-peer-exchange=true
      - --relay=true
      - --rest-address=0.0.0.0
      - --rest-admin=true
      - --rest-port=8646
      - --rest=true
#      - --rln-relay-chain-id=59141
#      - --rln-relay-cred-password=${RLN_RELAY_CRED_PASSWORD}
#      - --rln-relay-cred-path=/keystore/keystore.json
#      - --rln-relay-dynamic=false
#      - --rln-relay-tree-path=/data/rln_tree
#      - --rln-relay-eth-client-address=${RLN_RELAY_ETH_CLIENT_ADDRESS}
#      - --rln-relay-eth-contract-address=${RLN_CONTRACT_ADDRESS}
      - --rln-relay=false 
      - --staticnode=/dns4/nwaku/tcp/60002/p2p/16Uiu2HAmPnLx5wdrjCZKkbyuJo8CqM38cijf3aL9hGi7KuGabepb
      - --store-message-db-url=${STORE_MESSAGE_DB_URL}
      - --store=true
      - --tcp-port=60003
      - --websocket-port=60001
      - --websocket-support=true
#      - --discv5-bootstrap-node=enr:-QESuED0qW1BCmF-oH_ARGPr97Nv767bl_43uoy70vrbah3EaCAdK3Q0iRQ6wkSTTpdrg_dU_NC2ydO8leSlRpBX4pxiAYJpZIJ2NIJpcIRA4VDAim11bHRpYWRkcnO4XAArNiZub2RlLTAxLmRvLWFtczMud2FrdS5zYW5kYm94LnN0YXR1cy5pbQZ2XwAtNiZub2RlLTAxLmRvLWFtczMud2FrdS5zYW5kYm94LnN0YXR1cy5pbQYfQN4DgnJzkwABCAAAAAEAAgADAAQABQAGAAeJc2VjcDI1NmsxoQOTd-h5owwj-cx7xrmbvQKU8CV3Fomfdvcv1MBc-67T5oN0Y3CCdl-DdWRwgiMohXdha3UyDw
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9092/metrics || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    dns:
      - 8.8.8.8
      - 8.8.4.4

  prometheus:
    image: prom/prometheus:v2.51.2
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus-config.yml:/etc/prometheus/prometheus.yml:Z
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.size=5GB
    depends_on:
      - postgres-exporter
      - nwaku
      - nwaku2

  grafana:
    image: grafana/grafana:10.4.2
    ports:
      - "3000:3000"
    env_file:
      - ./monitoring/configuration/grafana-plugins.env
    volumes:
      - ./monitoring/configuration/grafana.ini:/etc/grafana/grafana.ini:Z
      - ./monitoring/configuration/dashboards.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml:Z
      - ./monitoring/configuration/datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:Z
      - ./monitoring/configuration/dashboards:/var/lib/grafana/dashboards/:Z
      - ./monitoring/configuration/customizations/custom-logo.svg:/usr/share/grafana/public/img/grafana_icon.svg:Z
      - ./monitoring/configuration/customizations/custom-logo.svg:/usr/share/grafana/public/img/grafana_typelogo.svg:Z
      - ./monitoring/configuration/customizations/custom-logo.png:/usr/share/grafana/public/img/fav32.png:Z
      - grafana-data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=MyNewSecret123!
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_NAME=Main Org. # Correct the Org Name here
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
    restart: on-failure
    depends_on:
      - prometheus
    dns:
      - 8.8.8.8
      - 8.8.4.4

volumes:
  grafana-data:

networks:
  default:
    driver: bridge
